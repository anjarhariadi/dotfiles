---
description: Save or update information in the right place in this Obsidian vault
---

Save this information into the vault, in the right place: $ARGUMENTS

Vault folder structure:
- 00_Personal — personal info, bio, preferences, identity
- 01_Learn — learning notes, courses, study material
- 02_Project — project docs, plans, specs
- 03_Work — work-related notes, meetings, tasks

Steps:
1. Decide which single folder this information belongs in.
2. Search existing files in that folder for one that already covers this topic — check filenames first, then skim content of likely candidates.
3. If a matching file exists: edit it rather than creating a new one. Insert the new info under an existing relevant heading if there is one, or add a new heading/section if not. Match the file's existing tone and formatting. Never duplicate information that's already there — if it's already covered, note that instead of writing anything.
4. If no matching file exists: create a new markdown file in the right folder with a short, descriptive, snake_case filename (e.g. about_me.md). Give it a top-level heading and write the info as clean markdown.
5. Report back: which file was created or edited, and a one-line summary of what was added/changed.
